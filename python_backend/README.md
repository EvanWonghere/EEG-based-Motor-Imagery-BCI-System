# python_backend (Legacy)

> **This directory contains early prototype scripts and is no longer actively maintained.**

The current implementation lives in `src/` (modular package) with CLI entry points in `scripts/`. See the [project README](../README.md) for up-to-date usage instructions.

These files are preserved for reference only.
