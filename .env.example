# Copy this file to .env and set values as needed.
# Do not commit .env (it is in .gitignore).

# ---------------------------------------------------------------------------
# MNE-Python data paths (used when downloading/loading datasets)
# ---------------------------------------------------------------------------
# Root directory for MNE datasets. Default is ~/mne_data if unset.
# Used by: mne.datasets.eegbci, sample data, and other MNE dataset loaders.
MNE_DATA=

# Optional: dataset-specific overrides (MNE checks these before MNE_DATA)
# Example for sample dataset: MNE_DATASETS_SAMPLE_PATH=/path/to/sample
# MNE_DATASETS_SAMPLE_PATH=
# For EEGBCI/PhysioNet, data is stored under MNE_DATA by default.

# ---------------------------------------------------------------------------
# Project paths (optional overrides; code uses project-relative paths if unset)
# ---------------------------------------------------------------------------
# DATA_DIR=./data
# MODELS_DIR=./models
