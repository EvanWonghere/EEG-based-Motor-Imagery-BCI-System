# 分阶段实施计划：从简陋版本到目标架构

> 基于 `项目架构设计.md` 的目标，将重构拆分为 6 个阶段
> 每个阶段结束后系统仍可正常运行（渐进式迁移，不破坏现有功能）

---

## 当前状态

```
python_backend/          # 扁平结构，~7 个文件
├── preprocessing.py     # 基本带通滤波 + 分段
├── training.py          # CSP+LDA/SVM/RF 流水线
├── train_model.py       # 入口脚本（500+ 行，数据加载+训练混杂）
├── datasets.py          # 数据集加载
├── analyze_model.py     # 模型分析
├── replay_stream.py     # LSL 回放
├── download_datasets.py # 数据下载
├── utils.py             # 常量与路径
└── test_datasets.py     # 数据集验证
```

**主要差距：**
- 无配置系统（参数硬编码）
- 无 CAR 重参考、ICA 伪迹去除、坏导检测
- 无 FBCSP（仅基础 CSP）
- 无 EEGNet 深度学习方法
- 无正式评估框架（无 Kappa、统计检验、跨方法对比）
- 可视化散落在各文件中
- Unity 前端仅基础接收

---

## 阶段零：基础设施搭建

**目标：** 建立新项目骨架、配置系统、工具模块，为后续迁移提供地基。

**产出：**
- [x] `src/` 包结构（`__init__.py` 文件）
- [x] `src/utils/config.py` — YAML 三层配置加载与合并
- [x] `src/utils/paths.py` — 路径管理
- [x] `src/utils/logging.py` — 统一日志
- [x] `src/utils/reproducibility.py` — 随机种子固定、环境快照
- [x] `configs/default.yaml` — 默认参数配置
- [x] `configs/datasets/bci_iv_2a.yaml`
- [x] `pyproject.toml` — 项目元数据

**验证标准：**
```bash
# 能成功加载配置
python -c "from src.utils.config import load_config; print(load_config('configs/default.yaml'))"
```

**注意事项：**
- 此阶段不动现有 `python_backend/` 的任何代码
- 新旧代码并行存在，`python_backend/` 保持可用

---

## 阶段一：数据管理层

**目标：** 将 `train_model.py` 和 `datasets.py` 中的数据加载逻辑抽象为统一接口。

**产出：**
- [x] `src/data/base.py` — `EEGDataset` 抽象基类
- [x] `src/data/bci_iv_2a.py` — BCI IV 2a 加载器
- [x] `src/data/bci_iv_2b.py` — BCI IV 2b 加载器
- [x] `src/data/download.py` — 数据下载（迁移自 `download_datasets.py`）
- [x] `src/data/__init__.py` — 数据集注册表 + `create_dataset()` 工厂
- [x] `scripts/download_data.py` — 新入口脚本
- [x] `tests/test_data.py` — 数据加载测试
- [x] `src/data/_compat.py` — NumPy 2.x 兼容层
- [x] `src/data/_mat_loader.py` — .mat 文件解析（提取自 train_model.py）

**验证标准：**
```bash
# 新加载器能正确读取数据，结果与旧代码一致
python -c "
from src.data import create_dataset
ds = create_dataset('bci_iv_2a', data_dir='data/')
raw = ds.load_raw(subject_id=1)
print(raw.info['sfreq'], len(raw.ch_names))
"
```

**依赖：** 阶段零（配置系统、路径管理）

---

## 阶段二：预处理流水线

**目标：** 将现有的基础预处理升级为完整流水线，新增 CAR、ICA、坏导检测等步骤。

**产出：**
- [x] `src/preprocessing/pipeline.py` — 流水线编排器
- [x] `src/preprocessing/filters.py` — 带通 / 陷波滤波
- [x] `src/preprocessing/reference.py` — 共平均参考（CAR）
- [x] `src/preprocessing/artifacts.py` — ICA 伪迹去除 + 坏段拒绝
- [x] `src/preprocessing/epochs.py` — 事件检测、分段、基线校正
- [x] `src/preprocessing/quality.py` — 坏导检测、数据质量报告
- [x] `tests/test_preprocessing.py`

**验证标准：**
```bash
# 流水线能端到端运行，输出干净的 Epochs
python -c "
from src.data import create_dataset
from src.preprocessing.pipeline import PreprocessingPipeline
from src.utils.config import load_config

cfg = load_config('configs/experiments/csp_lda_baseline.yaml')
ds = create_dataset('bci_iv_2a', data_dir='data/')
raw = ds.load_raw(1)
pipe = PreprocessingPipeline(cfg['preprocessing'])
epochs = pipe.run(raw, ds.get_event_id())
print(epochs)  # 应包含 ~144 个 epoch
"
```

**依赖：** 阶段一（数据管理层提供 `Raw` 数据）

---

## 阶段三：特征提取 + 分类模型

**目标：** 实现三条特征提取路线（CSP / FBCSP / 原始输入）和对应分类器，这是论文的核心。

### 3a：传统方法（CSP + FBCSP + 传统分类器）

**产出：**
- [x] `src/features/csp.py` — CSP 特征提取器
- [x] `src/features/fbcsp.py` — **FBCSP 特征提取器（重点）**
- [x] `src/features/band_power.py` — 频段功率特征
- [x] `src/features/selector.py` — 互信息特征选择
- [x] `src/models/base.py` — 模型基类 + 注册机制
- [x] `src/models/traditional.py` — LDA / SVM / RF
- [x] `configs/experiments/csp_lda_baseline.yaml`
- [x] `configs/experiments/fbcsp_svm.yaml`
- [x] `scripts/train.py` — 新训练入口（读 YAML → 训练 → 保存）
- [x] `tests/test_features.py`
- [x] `tests/test_models.py`

**验证标准：**
```bash
# CSP+LDA 基线 — 结果应与旧代码一致
python scripts/train.py --config configs/experiments/csp_lda_baseline.yaml --subject 1

# FBCSP+SVM — 应在大部分被试上优于 CSP+LDA
python scripts/train.py --config configs/experiments/fbcsp_svm.yaml --subject 1
```

### 3b：深度学习方法（EEGNet）

**产出：**
- [x] `src/models/eegnet.py` — EEGNet（PyTorch 实现）
- [x] `configs/experiments/eegnet.yaml`
- [x] 更新 `scripts/train.py` 支持深度学习训练循环
- [ ] ⚠️ EEGNet 运行验证（PyTorch 在 Python 3.14 上 segfault，需降级 Python 或等上游修复）

**验证标准：**
```bash
python scripts/train.py --config configs/experiments/eegnet.yaml --subject 1
# 应在 200 epoch 内收敛，准确率 > 60%
```

**依赖：** 阶段二（预处理流水线输出 Epochs）

---

## 阶段四：评估框架 + 可视化

**目标：** 建立完整的评估体系，自动生成论文所需的全部图表和统计数据。

### 4a：评估框架

**产出：**
- [ ] `src/evaluation/metrics.py` — Accuracy、Kappa、F1、ROC-AUC
- [ ] `src/evaluation/cross_validation.py` — 分层 K 折 / 跨会话 / LOSO
- [ ] `src/evaluation/statistics.py` — Wilcoxon、配对 t 检验、置信区间
- [ ] `src/evaluation/comparison.py` — Friedman + Nemenyi post-hoc 多方法对比
- [ ] `configs/experiments/all_methods_compare.yaml`
- [ ] `scripts/evaluate.py` — 评估入口
- [ ] `tests/test_evaluation.py`

### 4b：可视化

**产出：**
- [ ] `src/visualization/topo.py` — CSP 空间模式脑地形图
- [ ] `src/visualization/erds.py` — ERD/ERS 时频分析图
- [ ] `src/visualization/results.py` — 准确率柱状图、混淆矩阵、ROC 曲线、箱线图
- [ ] `src/visualization/report.py` — 自动实验报告生成
- [ ] `scripts/analyze.py` — 分析可视化入口

**验证标准：**
```bash
# 全方法对比实验
python scripts/evaluate.py --config configs/experiments/all_methods_compare.yaml

# 生成论文图表
python scripts/analyze.py --results-dir results/ --output-dir results/figures/
# results/figures/ 下应包含：accuracy_comparison.png, confusion_matrices.png,
# roc_curves.png, csp_topomaps.png, erds_maps.png, fbcsp_band_selection.png
```

**依赖：** 阶段三（需要已训练的模型和结果数据）

---

## 阶段五：在线仿真 + Unity 前端

**目标：** 重构 LSL 回放系统，升级 Unity 前端的试次流程和视觉反馈。

### 5a：Python 在线模块

**产出：**
- [ ] `src/online/lsl_sender.py` — LSL 数据/标记发送
- [ ] `src/online/online_pipeline.py` — 在线预处理 + 滑动窗口分类
- [ ] `src/online/feedback.py` — 置信度阈值、命令映射
- [ ] `scripts/run_online.py` — 在线仿真入口

### 5b：Unity 前端升级

**产出：**
- [ ] `unity_frontend/Assets/Scripts/LSL/LSLManager.cs` — 连接生命周期管理
- [ ] `unity_frontend/Assets/Scripts/LSL/MarkerReceiver.cs` — 标记接收与事件分发
- [ ] `unity_frontend/Assets/Scripts/Paradigm/TrialController.cs` — 完整试次流程
- [ ] `unity_frontend/Assets/Scripts/Paradigm/CueDisplay.cs` — 视觉提示
- [ ] `unity_frontend/Assets/Scripts/Feedback/FeedbackManager.cs` — 反馈可视化
- [ ] `unity_frontend/Assets/Scripts/Core/SessionManager.cs` — 会话管理
- [ ] `unity_frontend/Assets/Scenes/MainScene.unity` — 主仿真场景

**验证标准：**
```bash
# 启动 Python 端
python scripts/run_online.py --model results/models/best_fbcsp_svm_sub1.pkl

# Unity 端同步启动 → 能看到完整的 提示→想象→反馈→休息 循环
# 分类延迟 < 100ms
```

**依赖：** 阶段三（需要训练好的模型）、阶段四（可选，用于实时可视化）

---

## 阶段里程碑总览

```
阶段零 ─── 基础设施 ──────────────────────────────────────→ ✓ 配置可加载
  │
阶段一 ─── 数据管理 ──────────────────────────────────────→ ✓ 数据可读取
  │
阶段二 ─── 预处理流水线 ──────────────────────────────────→ ✓ Epochs 可输出
  │
阶段三 ─┬─ 3a: CSP/FBCSP + LDA/SVM ─────────────────────→ ✓ 传统方法可训练
  │      └─ 3b: EEGNet ──────────────────────────────────→ ✓ 深度学习可训练
  │
阶段四 ─┬─ 4a: 评估框架 ────────────────────────────────→ ✓ 统计数据可输出
  │      └─ 4b: 可视化 ─────────────────────────────────→ ✓ 论文图表可生成
  │
阶段五 ─┬─ 5a: Python 在线模块 ─────────────────────────→ ✓ LSL 可回放
         └─ 5b: Unity 前端 ─────────────────────────────→ ✓ 完整仿真可运行
```

---

## 迁移策略

1. **并行共存：** 整个过程中 `python_backend/` 保持不动，新代码写入 `src/` + `scripts/`。直到所有阶段完成并验证后，再一次性移除旧代码。
2. **逐模块验证：** 每个阶段完成后，用对应的验证标准确认功能正确，再进入下一阶段。
3. **配置优先：** 从阶段零开始就用 YAML 配置驱动，后续所有模块都从配置中读取参数，不硬编码。
4. **测试覆盖：** 每个阶段附带对应的 `tests/` 测试文件，确保重构不引入回归。

---

## 建议优先级

如果时间紧张，核心路径为：

> **阶段零 → 阶段一 → 阶段二 → 阶段三a（FBCSP+SVM）→ 阶段四a（评估）→ 阶段四b（可视化）**

这条路径覆盖了论文最核心的内容（第四、五、六章）。阶段三b（EEGNet）和阶段五（在线仿真）可在核心路径完成后再补充。
